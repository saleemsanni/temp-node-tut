const num1 = 5
const num2 = 10

function additioning() {
  console.log(`the sum is : ${num1 + num2}`)
}
additioning()

//if we have function instead of module that we INVOKE then the code will run even though we didnt assign it to a variable.
